{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "portfolio-project",
  "scripts": {
    "start": "parcel --open chrome index.html",
    "build": "parcel build index.html --dist-dir finalDir --no-scope-hoist"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.11.0",
    "@parcel/transformer-sass": "^2.11.0",
    "@parcel/transformer-webmanifest": "^2.11.0",
    "parcel": "^2.11.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.2",
    "jquery": "^3.7.1"
  }
}
